// import {} from '@/infra/orm/providers';
// import {} from '@/infra/orm/repositories';
import { Module, HttpModule } from '@nestjs/common';
import // usecases factories
'.';

@Module({
  providers: [
    // repositories
    // providers
    // factories
  ],
  exports: [
    // factories
  ],
  imports: [HttpModule],
})
export class FactoryModule {}
